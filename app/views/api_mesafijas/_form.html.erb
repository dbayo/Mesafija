<%= form_for(@api_mesafija) do |f| %>
  <% if @api_mesafija.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_mesafija.errors.count, "error") %> prohibited this api_mesafija from being saved:</h2>

      <ul>
      <% @api_mesafija.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
